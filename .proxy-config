# 代理配置文件
# 自动生成于 2025-01-17

HTTP_PROXY="http://127.0.0.1:7890"
HTTPS_PROXY="http://127.0.0.1:7890"
ALL_PROXY="socks5://127.0.0.1:7890"

# 使用说明:
# 1. 修改上面的代理地址为你自己的代理服务器
# 2. 如果不需要代理，将地址设置为空字符串
# 3. 支持的代理类型: http, https, socks5
# 4. 脚本会自动检测并使用这些配置

# 示例配置:
# HTTP_PROXY="http://127.0.0.1:7890"      # HTTP代理
# HTTPS_PROXY="http://127.0.0.1:7890"     # HTTPS代理
# ALL_PROXY="socks5://127.0.0.1:7890"     # SOCKS5代理

# 如果不需要代理，请设置为空:
# HTTP_PROXY=""
# HTTPS_PROXY=""
# ALL_PROXY=""
